# === Database Config ===
DB_USER=user
DB_PASSWORD=password
DB_DATABASE=app_db
# DATA_DIR_HOST=./backend/app/scripts  # 호스트 시드 데이터 경로(절대/상대 모두 가능), 컨테이너에서는 /app/seed-data로 고정 마운트

# === Auth (currently unused) ===
# SECRET_KEY=secret_key
# ACCESS_TOKEN_EXPIRE_MINUTES=60

# === App Host ===
APP_HOST=localhost
TEAM_SIZE=1
PENDING_TIMEOUT_MINUTES=1
